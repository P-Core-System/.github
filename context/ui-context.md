# UI Context

## Theme

Org metadata repo — **no product UI**. Rendered surfaces are the org profile
page (github.com/P-Core-System) and repo READMEs. Standard GitHub rendering.

## Colors / Typography / Components

N/A — GitHub renders markdown. No custom tokens, components, or skills needed.

## Layout Patterns

- Org profile: project family table (Project | Description | Language).
- READMEs: concise overview + feature list + commands + context note.

## Skills (UI / UX)

Not applicable — no product UI. If the org site grows beyond GitHub Pages,
use `ui-ux-pro-max` / `brand` / `design-system` and persist conventions here.